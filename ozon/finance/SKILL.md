---
name: finance
description: Финансовая аналитика Ozon с расчётом маржи, вычетов, убыточных SKU и отправкой дайджеста в Telegram.
---

# Ozon Finance Skill

## 📌 Назначение

# Скилл выполняет полный финансовый анализ продавца Ozon:

- Получение транзакций в реальном времени  
- Расчёт выручки и чистой прибыли  
- Разбор вычетов (комиссия, логистика, хранение, штрафы)  
- Расчёт маржи  
- Поиск убыточных и низкомаржинальных SKU  
- Отправка ежедневного и еженедельного дайджеста в Telegram  

---

## 🔐 Требования к конфигурации

В `config.yaml` должен быть раздел:

```yaml
ozon:
  enabled: true
  client_id: "YOUR_CLIENT_ID"
  api_key: "YOUR_API_KEY"

telegram:
  botToken: "YOUR_TELEGRAM_BOT_TOKEN"
  chatId: "YOUR_CHAT_ID"

finance:
  loss_margin_threshold: 0        # порог убыточности (%)
  low_margin_threshold: 20        # порог низкой маржи (%)

```

Если Telegram не указан — дайджест не отправляется.

# Входные параметры
**Основная функция**

process(config: dict, date_from: str, date_to: str) -> dict

Где:

date_from — формат "YYYY-MM-DD"

date_to — формат "YYYY-MM-DD"

**Пример:**

report = process(config, "2026-02-01", "2026-02-07")

# Выходные данные

Возвращается словарь:

{
  "gross_revenue": float,
  "net_revenue": float,
  "total_deductions": float,
  "margin_pct": float,
  "orders": int,
  "returns": int,
  "to_pay": float,
  "deductions_breakdown": {
      "commission": float,
      "logistics": float,
      "storage": float,
      "penalties": float,
      "other": float
  },
  "top_skus": [
      ("SKU123", {...})
  ],
  "loss_skus": list[str],
  "low_margin_skus": list[str],
  "by_sku": dict
}

Если Ozon отключён или транзакций нет — возвращается {}.

## Что анализируется
- Выручка
- Валовая выручка
- Чистая выручка
- Количество заказов
- Количество возвратов
- Вычеты
- Комиссия
- Логистика
- Хранение
- Штрафы
- Прочие удержания
- Маржинальность
- Общая маржа
- Убыточные SKU
- Низкомаржинальные SKU

## Telegram-дайджест

**Ежедневный**
send_daily_digest(config)

Берёт данные за вчера.

**Еженедельный**
send_weekly_digest(config)

Берёт прошлую неделю (пн–вс).

# В дайджесте:

- Баланс
- Выручка
- Чистая прибыль
- Маржа
- Вычеты подробно
- Топ-товар
- Флаги проблем (убытки, низкая маржа, высокий % возвратов)

## Используемые API Ozon

/v1/finance/balance

/v2/finance/realization

/v3/finance/transaction/list

/v3/finance/transaction/totals

## Особенности

Использует детальные транзакции вместо отчёта реализации (более актуальные данные)

Поддерживает пагинацию

Корректно работает без Telegram

Безопасен: ключи берутся только из конфигурации

## Роль в системе

Этот скилл — финансовое ядро аналитики.

# Рекомендуется использовать вместе с:

- ozon-analytics
- ozon-recommendations
Он является источником данных для управленческих решений.

---