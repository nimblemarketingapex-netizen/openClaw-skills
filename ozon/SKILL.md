---
name: ozon
description: Скилл для автоматизации работы продавца на маркетплейсе Ozon. Автоответы на отзывы и вопросы покупателей; финансовая аналитика и отчёт о реализации; аналитика продаж и остатков; ежедневный и еженедельный дайджест в Telegram; рекомендации по улучшению продаж; мониторинг транзакций и баланса.
---

Цель — помочь продавцу Ozon быстрее реагировать на клиентов и принимать решения на основе данных.

## Возможности

### 🤖 Автоответы на отзывы
- получение новых отзывов без ответа (`/v1/review/list`);
- генерация ответа через AI (OpenAI или локальный Ollama);
- отправка ответа (`/v1/review/comment/create`);
- фильтрация триггерных слов (угрозы, суд) → эскалация в Telegram.

### ❓ Ответы на вопросы покупателей
- получение новых вопросов (`/v1/question/list`);
- генерация ответа через AI;
- отправка ответа (`/v1/question/answer/create`);
- топ-SKU по количеству вопросов (`/v1/question/top-sku`).

### 💰 Финансы
- отчёт о реализации за период (`/v2/finance/realization`);
- детализация по дням (`/v1/finance/realization/by-day`);
- список транзакций с разбивкой (`/v3/finance/transaction/list`);
- итоги транзакций (`/v3/finance/transaction/totals`);
- текущий баланс (`/v1/finance/balance`);
- расчёт реальной маржи, вычетов, суммы к выплате;
- выявление убыточных SKU.

### 📊 Аналитика продаж
- данные по продажам (`/v1/analytics/data`);
- остатки на складах (`/v1/analytics/stock_on_warehouses`, `/v2/analytics/stock_on_warehouses`);
- оборачиваемость товаров (`/v1/analytics/item_turnover`);
- среднее время доставки (`/v1/analytics/average-delivery-time`);
- поисковые запросы по товарам (`/v1/analytics/product-queries`).

### 🏬 Склад и остатки
- остатки по складам FBO/FBS (`/v3/product/info/stocks`, `/v4/product/info/stocks`);
- остатки по складам FBS (`/v2/product/info/stocks-by-warehouse/fbs`);
- товары с риском дефицита;
- залежавшиеся позиции.

### 📬 Дайджест в Telegram
- ежедневный отчёт: баланс, выручка, заказы, возвраты, флаги проблем;
- еженедельный расширенный отчёт: топ-5 SKU, детализация транзакций;
- алерты: убыточные SKU, высокий % возвратов, штрафы.

### 💡 Рекомендации
- на основе аналитики продаж и склада;
- по управлению акциями (`/v1/actions`, `/v1/seller-actions/list`);
- по ценообразованию и ценовым стратегиям.

---

## Требования

- Python 3.10+
- `requests` библиотека
- Ozon Seller API: **Client ID** + **API Key**
- Токен должен иметь роли: `Review`, `Question`, `Report`, `Product read-only`
- Telegram Bot Token + Chat ID для дайджестов и эскалаций
- OpenAI API Key или локальный Ollama (опционально, для AI-ответов)

---

## Аутентификация Ozon API

Ozon использует два заголовка вместо одного токена:

```http
Client-Id: 1224747
Api-Key: ваш-api-key
Content-Type: application/json
```

Все запросы к Ozon Seller API — **POST** с JSON-телом (не GET с params).

---

## Конфигурация

```json
{
  "ozon": {
    "enabled": true,
    "client_id": "1224747",
    "api_key": "ваш-api-key"
  },
  "openai": {
    "apiKey": "ваш-openai-key"
  },
  "telegram": {
    "botToken": "ваш-bot-token",
    "chatId": "ваш-chat-id"
  },
  "finance": {
    "loss_margin_threshold": 0,
    "low_margin_threshold": 20
  }
}
```

---

## Ключевые отличия от Wildberries API

| Параметр | Wildberries | Ozon |
|---|---|---|
| Аутентификация | 1 токен в заголовке `Authorization` | `Client-Id` + `Api-Key` в заголовках |
| Метод запросов | GET с query params | **POST с JSON-телом** |
| База URL | несколько доменов | `https://api-seller.ozon.ru` |
| Пагинация | через `page` / `rrdid` | через `last_id` / `page_token` / `offset` |
| Отзывы | feedbacks-api.wildberries.ru | api-seller.ozon.ru/v1/review/* |
| Финансы | `/api/v5/supplier/reportDetailByPeriod` | `/v2/finance/realization` + `/v3/finance/transaction/list` |

---

## Роли токенов (из официальной документации)

Для работы скилла нужны следующие роли:

| Роль | Что даёт | Нужные эндпоинты |
|---|---|---|
| `Review` | Отзывы: чтение и управление | `/v1/review/*` |
| `Question` | Вопросы: чтение и ответы | `/v1/question/*` |
| `Report` | Аналитика и финансовые отчёты | `/v1/analytics/*`, `/v2/finance/realization`, `/v3/finance/transaction/*` |
| `Product read-only` | Информация о товарах и остатках | `/v3/product/info/stocks`, `/v4/product/info/stocks` |

---

## Структура проекта

```
ozon/
├── SKILL.md              ← этот файл
├── main.py               ← оркестратор
├── autoanswers/
│   ├── SKILL.md
│   └── script.py         ← отзывы + вопросы
├── analytics/
│   ├── SKILL.md
│   └── script.py         ← аналитика продаж и склада
├── finance/
│   ├── SKILL.md
│   └── script.py         ← финансы, транзакции, дайджест
└── recommendations/
    ├── SKILL.md
    └── script.py         ← рекомендации
```

---

## Как вызывать

```python
from ozon.main import run_ozon_skill
from ozon.finance.script import send_daily_digest

# Полный цикл
results = run_ozon_skill(config, date_from="2025-02-17", date_to="2025-02-23")

# Ежедневный дайджест (крон)
send_daily_digest(config)
```

---

## Расписание (cron)

| Задача | Когда | Функция |
|---|---|---|
| Ежедневный дайджест | Каждый день 09:00 UTC | `send_daily_digest(config)` |
| Еженедельный отчёт | Каждый понедельник 09:00 UTC | `send_weekly_digest(config)` |
| Автоответы на отзывы | Каждые 30 минут | `process_reviews(config)` |
| Ответы на вопросы | Каждые 30 минут | `process_questions(config)` |

---

## Примечания

- Все запросы — POST с JSON, не GET. Это главное отличие от WB.
- Client ID и API Key хранятся только в конфиге/переменных окружения.
- Финансовый отчёт `/v2/finance/realization` возвращает данные за **закрытые периоды** — текущий месяц может быть неполным.
- Транзакции `/v3/finance/transaction/list` дают актуальную картину в реальном времени.
- Для сервиса «Бухгалтерия» нужна отдельная роль токена.