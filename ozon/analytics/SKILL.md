---
name: analytics
description: Аналитика продаж, остатков и поисковых запросов Ozon с выявлением дефицита и топ-товаров.
---

# Ozon Analytics Skill

## 📌 Назначение

Скилл собирает и агрегирует аналитику продавца Ozon:

- Продажи за период  
- Выручка и средний чек  
- Возвраты  
- Топ-SKU  
- Остатки по складам  
- Дефицит и нулевые остатки  
- Топ поисковых запросов  

**Этот модуль является источником данных для:**
- `ozon-recommendations`
- отчётов
- dashboard
- Telegram-бота аналитики

---

## 🔐 Конфигурация

В `config.yaml`:

```yaml
ozon:
  enabled: true
  client_id: "YOUR_CLIENT_ID"
  api_key: "YOUR_API_KEY"
```

Без включённого ozon.enabled скилл возвращает {}.

# Точка входа

process(config: dict, date_from: str, date_to: str) -> dict

Параметры:

date_from — формат "YYYY-MM-DD"

date_to — формат "YYYY-MM-DD"

**Пример:**

```yaml
analytics = process(config, "2026-02-01", "2026-02-07")
📤 Формат результата
{
  "sales": {
      "orders": int,
      "revenue": float,
      "returns": int,
      "avg_check": float,
      "top_sku": [
          ("SKU123", {
              "orders": int,
              "revenue": float,
              "returns": int
          })
      ],
      "by_sku": dict,
      "by_date": dict
  },
  "stock": {
      "total_skus": int,
      "stocks": {
          "SKU123": {
              "free": int,
              "reserved": int,
              "total": int
          }
      },
      "low_stock": list[str],
      "zero_stock": list[str]
  },
  "top_queries": list,
  "period": {
      "from": str,
      "to": str
  }
}

```

## Что анализируется
# Продажи (/v1/analytics/data)

**Метрики:**
- ordered_units
- revenue
- returns
- cancellations
- delivered_units

**Группировка:**
- по SKU
- по дням

**Рассчитывается:**
- общие заказы
- общая выручка
- возвраты
- средний чек
- топ-5 SKU по выручке

# Склад (/v2/analytics/stock_on_warehouses)

**Определяется:**

- Нулевой остаток (free_to_sell = 0)
- Низкий остаток (free_to_sell < 5)
- Нормальный запас
- Работает для FBO и FBS.

# Поисковые запросы (/v1/analytics/product-queries)

**Возвращает топ запросов, по которым покупатели находят товары продавца.**

Используется для:

- SEO-оптимизации карточек
- расширения ассортимента
- анализа спроса

# Используемые API Ozon

/v1/analytics/data

/v2/analytics/stock_on_warehouses

/v1/analytics/product-queries

## Особенности

- Поддержка пагинации (offset)
- Агрегация по SKU и дням
- Безопасная работа при ошибках API
- Не требует Telegram
- Не выполняет внешних AI-запросов

## Роль в системе

Этот скилл — операционная аналитика.

# Обычно используется в связке:

- ozon-analytics → собирает данные
- ozon-finance → считает деньги
- ozon-recommendations → даёт управленческие решения

## Рекомендации по использованию

Лучший сценарий:
- ежедневный запуск по cron
- недельный агрегированный отчёт
- передача данных в ozon-recommendations
- визуализация в dashboard
---