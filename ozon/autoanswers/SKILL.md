---
name: autoanswers
description: Автоответы на отзывы и вопросы покупателей Ozon с AI-генерацией и эскалацией триггерных обращений в Telegram.
---

# Ozon AutoAnswers Skill

## 📌 Назначение

Скилл автоматизирует коммуникацию с покупателями:

- Получает новые отзывы без ответа  
- Генерирует ответ через AI (OpenAI или локальная модель Ollama)  
- Отправляет ответ в Ozon  
- Обрабатывает вопросы покупателей  
- Фильтрует триггерные слова (суд, жалобы и т.п.)  
- Эскалирует сложные случаи в Telegram  

---

## 🔐 Конфигурация

В `config.yaml`:

```yaml
ozon:
  enabled: true
  client_id: "YOUR_CLIENT_ID"
  api_key: "YOUR_API_KEY"

telegram:
  botToken: "YOUR_TELEGRAM_BOT_TOKEN"
  chatId: "YOUR_CHAT_ID"

openai:
  apiKey: "YOUR_OPENAI_API_KEY"   # необязательно
  ```
AI-режим работы

Если указан openai.apiKey → используется OpenAI

## Если ключа нет → используется локальная модель через Ollama
(ожидается http://localhost:11434
)

Если ни один AI недоступен — отправляется безопасный шаблонный ответ.

## Логика генерации ответа

**System prompt:**

Ты помощник продавца на маркетплейсе Ozon.
Отвечай вежливо, по делу, не более 3 предложений.

# Ограничения:

- кратко
- без агрессии
- без юридических оценок
- без признания вины

# Триггерные слова

Если отзыв или вопрос содержит слова:

- суд
- полиция
- прокуратура
- жалоба
- роспотребнадзор
- мошенники
- верните деньги
- адвокат
- штраф
- и др.

то:

✔ Ответ НЕ отправляется
✔ Сообщение пересылается в Telegram
✔ Требуется ручная обработка

## Обработка отзывов

**Используются API Ozon:**

/v1/review/list

/v1/review/comment/create

Точка входа
process_reviews(config: dict) -> list

Возвращает список действий:

[
  {"id": "123", "answered": True},
  {"id": "456", "action": "sent_to_telegram"}
]

## Обработка вопросов

Используются API:

/v1/question/list

/v1/question/answer/create

Точка входа
process_questions(config: dict) -> list

Работает аналогично отзывам.

## Telegram-эскалация

**Если найден триггер:**

Отправляется сообщение вида:

⚠️ Ozon — триггерный отзыв

Текст обращения...

ID: XXXXX

Ответ покупателю в этом случае не публикуется.

🔄 Используемые API Ozon

/v1/review/list

/v1/review/comment/create

/v1/question/list

/v1/question/answer/create

# Поведение при ошибках

- Если Ozon отключён → возврат []
- Если AI недоступен → шаблонный ответ
- Если Telegram не настроен → эскалация пропускается
- Логирование через logging

## Роль в системе

**Этот скилл — коммуникационный слой продавца.**

# Рекомендуется использовать вместе с:

- ozon-finance
- ozon-analytics
- ozon-recommendations

**Позволяет:**
- автоматизировать ответы
- снизить нагрузку на поддержку
- контролировать юридические риски

---