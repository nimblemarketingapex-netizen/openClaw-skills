---
name: figma
description: Скилл для анализа дизайна и получения данных из :contentReference[oaicite:0]{index=0} через backend. Используется для обработки ссылок на дизайн, извлечения структуры, стилей и подготовки данных для генерации кода или анализа.
---

# Figma Design Analysis & Code Generation Skill

## 🎯 Цель

Данный skill принимает **Figma ссылку** (или fileKey), получает данные из Figma через backend,  
и возвращает:

✔ структурированный JSON с данными дизайна  
✔ краткий обзор содержимого  
✔ важные элементы, цвета, текстовые стили  
✔ по запросу — сгенерированный код (React/HTML/другой)

Skill создан для MVP **Figma-AI помощника**, в котором:

1) Пользователь логинится через Figma OAuth  
2) Пользователь вводит ссылку на Figma дизайн  
3) Скилл получает данные через backend API  
4) Анализирует дизайн и генерирует вывод

---

## 📥 Входные параметры

| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| `user_id` | string | да | Идентификатор пользователя (для token из базы) |
| `fileKey` | string | да | Ключ Figma-файла (из URL Figma) |
| `mode` | string | нет | Тип анализа (например `summary`, `code:react`, `design`) |

---

## 📤 Выход

Skill возвращает объект с:

✔ общим описанием дизайна  
✔ основными объектами (фреймы/компоненты)  
✔ ключевыми стилями (цвета, тексты, размеры)  
✔ код (опционально), если mode = code:*

---

## 🧠 Логика

1. Получить из backend данные Figma (через `/figma/file/:fileKey`)  
2. Структурировать JSON  
3. Сделать анализ:  
   - ключевые фреймы  
   - цвета, текстовые стили  
   - имена компонентов  
4. Сгенерировать код, если нужно  
5. Вернуть результат

---

## 📌 Примеры использования

### Пример A — краткий обзор дизайна

**Вход**:  
```json
{
  "user_id": "default",
  "fileKey": "abcd1234",
  "mode": "summary"
}
```
**Выход**:  
```json
{
  "summary": "Дизайн представляет собой мобильное приложение для заказа еды. На главном экране есть список ресторанов с изображениями и рейтингами. Цветовая палитра включает яркие оттенки красного и оранжевого, а также нейтральные серые тона. Используются крупные шрифты для заголовков и более мелкие для описаний.",
  "frames": [
    {
      "name": "Main Screen",
      "type": "FRAME",
      "children": [
        {
          "name": "Restaurant List",
          "type": "GROUP",
          "children": [...]
        },
        ...
      ]
    },
    ...
  ],
  "colors": [
    {
      "name": "Primary Red",
      "hex": "#FF5733"
    },
    ...
  ],
  "textStyles": [
    {
      "name": "Heading 1",
      "fontFamily": "Roboto",
      "fontSize": 24,
      "fontWeight": 700
    },
    ...
  ]
}
```
### Пример B — генерация React компонента
**Вход**:  
```json
{
  "user_id": "default",
  "fileKey": "abcd1234",
  "mode": "code:react"
}
```
**Выход**:
```json
{
  "code": "export function Card() { ... }"
}
```
---

## ❓ Ошибки

Если токен отсутствует:
{
error: "Token not found — please authorize via Figma."
}


Если backend вернул ошибку Figma API — возвращается полный текст ошибки.

---

## 📦 Замечания

- Skill работает через backend — нет прямых вызовов Figma API из OpenClaw
- Skill универсален — поддерживает разные форматы ответа
- Дополнительно можно расширить под GraphQL / image export
- Все сгенерированные примеры HTML/код‑файлов по умолчанию сохраняются в папку `skills/figma/examples` внутри workspace (`__dirname + "/examples"` на уровне handler.js). Имя файла можно задавать явно (например, `sales-dashboard.html`).

This skill uses the tool "run" to fetch data from backend.
